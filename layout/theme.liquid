
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ shop.name }}</title> 
    <link rel="stylesheet" src="{{ theme.css | asset_url }}">
    {{ content_for_header }} 
</head>
<body>
    {% section "header" %}
    {{ content_for_layout }}    
    {% section "footer" %}
    <script src="{{ theme.js | asset_url }}" defer></script>
</body>
</html>


